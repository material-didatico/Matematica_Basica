%------------------------------------------------------------------------------%
\chapter{Trigonometria e Funções Trigonométricas}

Uma importante parte da Matemática é o estudo dos ângulos e das relações entre
ângulos e distâncias. Para ilustrar essa importância utilizaremos o artigo a
seguir que exemplifica como os conceitos da Trigonometria podem ser utilizados
para resolver problemas da vida real.

%------------------------------------------------------------------------------%
\subsection{Eratóstenes e a circunferência da Terra}

\begin{flushright}
\color{red}
JOSÉ ROBERTO V. COSTA
\end{flushright}

Eratóstenes nasceu em Cirene, Grécia, e morreu em Alexandria, Egito, no
terceiro século AEC. Ele era bibliotecário-chefe da famosa Biblioteca de
Alexandria, e foi lá que ele encontrou, num velho papiro, indicações de que ao
meio-dia de cada 21 de junho na cidade de Assuã (ou Syene, no grego antigo) 800
km ao sul de Alexandria, uma vareta fincada verticalmente no solo não produzia
sombra.

Cultura inútil, diriam alguns. Não para um homem observador como Eratóstenes.
Ele percebeu que o fenômeno não ocorria no mesmo dia e horário em Alexandria, e
pensou: Se o mundo é plano como uma mesa, então as sombras das varetas têm de ser
iguais. E se isto não acontece é porque a Terra deve ser curva!

\begin{figure}
  \centering
  \includegraphics[width=0.8\textwidth]{siena}
  \caption{Siena}
\end{figure}

Mais do que isso. Quanto mais curva fosse a superfície da Terra, maior seria a
diferença no comprimento das sombras.

O Sol deveria estar tão longe que seus raios de luz chegam à Terra paralelos.
Varetas fincadas verticalmente no chão em lugares diferentes lançariam sombras
de comprimentos distintos.

Eratóstenes decidiu fazer um experimento. Ele mediu o comprimento da sombra em
Alexandria ao meio-dia de 21 de junho, quando a vareta em Assuã, ao Sul do
Egito, não produzia sombra. Assim, ele obteve o ângulo $A$, conforme a figura
abaixo.

\begin{figure}
  \centering
  \includegraphics[height=4cm,width=0.4\textwidth]{siena2}
  \qquad
  \includegraphics[height=4cm,width=0.2\textwidth]{siena3}
  \caption{Siena 2 e 3}
\end{figure}

Preste atenção na figura acima. O ângulo $B$ terá o mesmo valor de
$A$, pois o desenho do experimento de Eratóstenes se reduz a uma
geometria muito simples: \textit{se duas retas paralelas interceptam uma reta
transversal, então os ângulos correspondentes são iguais}.

As retas paralelas são os raios de luz do Sol e a reta transversal é a que
passa pelo centro da Terra e pela vareta em Alexandria. O ângulo $B$ (também
igual a \ang{7}), é a uma fração conhecida da circunferência da Terra e
corresponde à distância entre Assuã e Alexandria!

Eratóstenes sabia que essa distância valia cerca de \SI{800}{\kilo\meter}
e então pensou: \ang{7} são aproximadamente 1/50 de uma circunferência (\ang{360}).
E isso corresponde a cerca de 800 km. Oitocentos quilômetros vezes cinquenta são
quarenta mil quilômetros, de modo que deve ser este o valor da
circunferência da Terra.

Valor encontrado atualmente: cerca de 40.072 km ao longo da linha do equador.
Um erro muito pequeno para uma medida tão simples, e feita há tanto tempo! Com
a circunferência, podemos calcular o diâmetro e o raio ou ainda o volume e a
área da superfície, mediante fórmulas simples.

Repare que o conhecimento utilizado por Eratóstenes (retas paralelas cortadas
por uma transversal) é formalmente adquirido hoje nas aulas de geometria do
Ensino Fundamental.

{\color{red}
  Fonte: Costa, J. R. V. Eratóstenes e a circunferência da Terra. Astronomia no
  Zênite, jul 2000. Disponível em:
  \url{http://www.zenite.nu/eratostenes-e-a-circunferencia-da-terra}
}

%------------------------------------------------------------------------------%
\section{O conceito de Ângulo}

Um \emph{ângulo} é determinado por uma abertura entre duas retas de mesma
origem. Para medirmos ângulos precisamos estabelecer o valor do ângulo total
(uma volta completa no sentido anti-horário) e os demais ângulos são medidos de
maneira proporcional. Existem duas formas usuais de medirmos ângulos: definindo
o ângulo total como 360 \emph{graus} ou como $2\pi$ \emph{radianos}.
Vejamos algumas medições em cada um dos dois sistemas.

\begin{figure}
  \centering
  \includegraphics[width=0.3\textwidth,trim={0 0 10,5cm 1cm},clip]{circ1}
  \qquad
  \includegraphics[width=0.3\textwidth,trim={0 0 10,5cm 1cm},clip]{circ2}
  \caption{Círculo 1 e 2}
\end{figure}

Considere um ângulo $\theta$. Denotamos por $\theta_g$ a medida desse ângulo em
graus e $\theta_r$ em radianos. Esses dois valores estão relacionados pela
equação. Isso quer dizer que podemos obter $\theta_g$ se conhecemos
$\theta_r$ ou vice versa.
\[
  \frac{360}{2\pi} = \frac{\theta_g}{\theta_r}.
\]

A unidade de medida padrão no ensino superior é radianos e para transformar
para radianos é comum usar a relação equivalente $\ang{180}=\pi rad$,
conforme ilustra o exemplo que segue.

\begin{example}
  Converta $120^{\circ}$ para radianos.

  \solution
  Para fazermos essa conversão usamos que \ang{180} é equivalente
  a $\pi rad$ e montamos a seguinte regra de três.
  \[
    \begin{array}{c<{\quad}c}
      \text{graus} & \text{radianos} \\
      \ang{180}    & \pi rad         \\
      \ang{120}    & x
    \end{array}
  \]
  Assim,
  \begin{align*}
    180 x & = 120\pi                       \\
    x     & = \frac{2 \pi}{3} \text{ rad}.
  \end{align*}
  Logo, \ang{120} é equivalente a \(\frac{2 \pi}{3}\) rad.
\end{example}

Pode ser que seja necessário voltar de radianos para graus.
O exemplo seguinte ilustra essa situação.

\begin{example}
 Converta $5\pi/3$ radianos para graus.

 \solution
 Para converter de radianos para graus, basta usarmos a relação
 $\ang{180} = \pi$ rad. Assim,
 \[
   \frac{5\pi}{3} rad
   = 5 \frac{\ang{180}}{3}
   = \ang{300}.
 \]
\end{example}

%------------------------------------------------------------------------------%
\section{Relações no Triângulo Retângulo}

Importantes relações são estabelecidas entre os ângulos e os lados de
triângulos retângulos. Considere um triângulo retângulo que contém um ângulo
$0<\theta<\pi/2$ conforme Figura~\ref{fig:triangulo-retangulo}.

\begin{figure}
  \centering
  \includegraphics[width=0.5\textwidth,trim={1cm 20cm 19cm 9cm},clip]{figdeflados}
  \caption{Triangulo retângulo}
  \label{fig:triangulo-retangulo}
\end{figure}

Em relação ao ângulo $\theta$, os lados desse triângulo recebem os seguintes nomes:
\begin{itemize}
  \item cateto oposto
  \item cateto adjacente
  \item hipotenusa
\end{itemize}

Como a soma dos ângulos internos de qualquer triângulo é $\pi$ radianos, o
terceiro ângulo (que chamaremos de $\alpha$) desse triângulo mede
$\pi/2-\theta$ (pois $\alpha+\theta+\pi/2=\pi$). Portanto, o cateto oposto ao
ângulo $\theta$ é o cateto adjacente ao ângulo $\alpha$ e o cateto adjacente ao
ângulo $\theta$ é o cateto oposto ao ângulo $\alpha$.

Estudaremos as relações entre os lados de dois triângulos retângulos que
possuem um mesmo ângulo $\theta$. Fixamos dois desses triângulos e nomeamos os
lados desses triângulos da seguinte forma:

\begin{figure}
  \centering
  \includegraphics[width=0.5 \textwidth,trim={1cm 28cm 17cm 1cm} ,clip]{figdeffunc}
  \caption{?}
\end{figure}

Como ambos os triângulos possuem um ângulo reto e um ângulo $\theta$, o
terceiro ângulo desses dois triângulos mede $\pi/2-\theta$. Ou seja, eles são
semelhantes pelo caso $AAA$ (ângulo ângulo ângulo) e podemos estabelecer as
seguintes relações:
\[
  \frac{y_1}{z_1} = \frac{y_2}{z_2},  \qquad
  \frac{x_1}{z_1} = \frac{x_2}{z_2},  \qquad
  \frac{y_1}{x_1} = \frac{y_2}{x_2}.
\]

Ou seja, independente dos valores dos catetos e da hipotenusa, as razões entre
os lados de um triângulo retângulo que contém um ângulos $\theta$ são
constantes. Essas constantes (que dependem de $\theta$) recebem nomes, os
principais deles são: (Outras razões recebem os nomes de secante,
cosseno e cotangente.)
\begin{description}
  \item[Seno de $\theta$]:
        \(\sin\theta=\frac{\text{cateto oposto}}{\text{hipotenusa}}\).

  \item[Cosseno de $\theta$]:
        \(\cos\theta=\frac{\text{cateto adjacente}}{\text{hipotenusa}}\).

  \item[Tangente de $\theta$]:
        \(\tan\theta=\frac{\text{cateto oposto}}{\text{cateto adjacente}}\).
\end{description}

Assim definidos, seno, cosseno e tangente são funções cujo domínio é o
intervalo $(0, \pi/2)$. Para estendermos essas definições e obtermos funções com
domínio real precisaremos generalizar a noção de ângulos para valores maiores
que $2\pi$ e ângulos negativos e posteriormente, estender a noção de seno,
cosseno e tangente desses ângulos através do círculo trigonométrico unitário.

\begin{observation}
  É possível calcular os valores de seno, cosseno e tangente para os ângulos
  $\pi/4$, $\pi/3$ e $\pi/6$ utilizando a relação de Pitágoras no quadrado de
  lado 1 e no triângulo equilátero de lado 1. Esses ângulos são chamados de
  notáveis porque para demais ângulos não existe uma maneira tão simples de
  estabelecer os valores de seno, cosseno e tangente.
\end{observation}

\begin{figure}
  \centering
  \includegraphics[scale=0.4]{quadrado}
  \caption{Quadrado de lado 1}
  \label{fig:quadrado}
\end{figure}

\begin{example}
  Calcule os valores de seno, cosseno e tangente para os ângulos $\pi/4$,
  $\pi/3$ e $\pi/6$ utilizando a relação de Pitágoras no quadrado de lado 1 e
  no triângulo equilátero de lado 1, justificando a tabela a seguir.

  \solution
  Vamos mostrar as razões trigonométricas para o ângulo de \ang{45}. Para
  isso, considere um quadrado de lado 1 e tracemos sua diagonal. Ao fazermos
  esse procedimento, dividimos o quadrado em dois triângulos retângulos iguais
  como mostra a Figura~\ref{fig:quadrado}.

  Podemos observar que:
  \begin{itemize}
      \item Cateto oposto a    \ang{45} vale 1;
      \item Cateto adjacente a \ang{45} vale 1;
      \item Hipotenusa desses triângulos retângulos é $\sqrt{2}$.
  \end{itemize}
  Assim,
  \begin{align*}
     \sin(\ang{45}) & = \frac{1}{\sqrt{2}} = \frac{\sqrt{2}}{2}; \\[2mm]
     \cos(\ang{45}) & = \frac{1}{\sqrt{2}} = \frac{\sqrt{2}}{2}; \\[2mm]
     \tan(\ang{45}) & = \frac{1}{1}        = 1.
  \end{align*}
  Para encontrarmos as razões trigonométricas para os ângulos de \ang{30} e
  \ang{60} basta traçarmos uma altura (que é mediana e bissetriz) em um
  triângulo equilátero de lado $l$.
\end{example}

%------------------------------------------------------------------------------%

A partir dos valores encontrados no Exercício 1.2, construímos a seguinte
tabela
\begin{ceqn}
\[
  \begin{array}{c<{\quad}>{\quad}c<{\quad}>{\quad}c<{\quad}>{\quad}c}  \hline
    \theta          & \sin\theta          & \cos\theta          & \tan\theta          \\ \hline
    \dfrac{\pi}{6}  & \dfrac{1}{2}        & \dfrac{\sqrt{3}}2{} & \dfrac{\sqrt{3}}{3} \\[3mm]
    \dfrac{\pi}{4}  & \dfrac{\sqrt{2}}{2} & \dfrac{\sqrt{2}}{2} &      1              \\[3mm]
    \dfrac{\pi}{3}  & \dfrac{\sqrt{3}}{2} & \dfrac{1}{2}        &  \sqrt{3}           \\[3mm] \hline
  \end{array}
\]
\end{ceqn}

A seguir resolveremos alguns exercícios que envolvem as razões métricas em um
triângulo retângulo.

\begin{figure}
  \centering
  \includegraphics[scale=0.5]{exerc1t}
  \caption{Ilustração exercício \ref{trigon1ex}.}
  \label{exercit1}
\end{figure}

\begin{example} \label{trigon1ex}
  Um avião decola, percorrendo uma trajetória retilínea, formando com o solo,
  um ângulo de \ang{30} (suponha que a região sobrevoada pelo avião seja
  plana). Após percorrer 1000 metros, qual a altura atingida pelo avião?

  \solution
  A figura \ref{exercit1} descreve a situação.

  Note que temos envolvidos no problema um ângulo (\ang{30}), o cateto
  oposto a esse ângulo ($h$) e a hipotenusa (\SI{1000}{\meter}), assim,
  é conveniente usar o seno para obtermos a medida desconhecida.
  \begin{align*}
    \sin(\ang{30}) & = \frac{h}{1000}   \\
    \frac{1}{2}    & = \frac{h}{1000}   \\
    h              & = \SI{500}{\meter}
  \end{align*}
  Assim, após percorrer \SI{1000}{\meter} o avião atinge \SI{500}{\meter}.
\end{example}

\begin{figure}
  \centering
  \includegraphics[width=0.5\textwidth,trim={3cm 13cm 3cm 15cm},clip]{figexercar}
  \caption{Figura para o exercício \ref{exerc34}.}
\end{figure}

\begin{example} \label{exerc34}
  Um sensor detecta a aproximação de veículos em uma rua. Determine uma função
  $x(\theta)$ que forneça a distância do sensor ao veículo em relação a
  $\theta$. Se o sensor só detecta objetos quando $\theta$ é menor ou igual a
  $\pi/3$, determine a distância máxima que o carro pode estar do sensor para
  ser detectado.

  \solution
  A distância máxima ocorre quando o ângulo no qual o sensor detecta os objetos
  é máxima. Como, no exercício, temos o valor do cateto adjacente ao ângulo
  $\theta$ e queremos encontrar a hipotenusa. Portanto, iremos usar o cosseno.
  \begin{align*}
    \cos(\ang{60}) & = \frac{8}{x}     \\
    \frac{1}{2}    & = \frac{8}{x}     \\
    x              & = \SI{16}{\meter}
  \end{align*}
  Portanto, a distância máxima que o carro pode estar do sensor para ser
  detectado é de \SI{16}{\meter}.
\end{example}

%------------------------------------------------------------------------------%
\section{Generalização da Noção de Ângulo}

Após uma volta completa, continuamos medindo os ângulos. Assim, por exemplo, o
ângulo $\pi/6$ ocupa a mesma posição que os ângulos $2\pi+\pi/6$ (após uma
volta) ou $4\pi+\pi/6$ (após duas voltas) e assim por diante. Mais geralmente,
para qualquer $n$ natural, os ângulos $x$ e $x+2\pi n$ ocupam a mesma posição
no círculo trigonométrico.

Para dar sentido a ângulos negativos, convenciona-se que ao girarmos no sentido
anti-horário, medimos os ângulos negativamente.

\begin{figure}
  \centering
  \includegraphics[width=0.3\textwidth,trim={0 0 10,5cm 1cm},clip]{circ3}
\end{figure}

Portanto, um ângulo $x$ radianos ocupa o mesmo lugar que todos os ângulos da forma
\[
  x+2\pi n, \quad n\in\Z.
\]

%------------------------------------------------------------------------------%
\section{Generalização das Funções Trigonométricas: O círculo trigonométrico unitário}

Considere o triângulo retângulo de hipotenusa 1, apresentado na Figura~\ref{trigon1}.

\begin{figure}
  \centering
  \includegraphics[width=0.6\textwidth,trim={3cm 13cm 3cm 15cm},clip]{figcirctrig1}
  \caption{Triângulo retângulo de hipotenusa 1.}
  \label{trigon1}
\end{figure}

Suponhamos que
\begin{itemize}
  \item o triângulo esteja situado no plano cartesiano,

  \item  o vértice associado ao ângulo $\theta$ esteja sobre a origem $(0, 0)$,

  \item o cateto adjacente ao ângulo $\theta$ fique sobre a parte positiva do
    eixo $x$,

  \item  a hipotenusa do triângulo retângulo meça 1 e o ângulo $\theta$ seja
    medido no sentido anti-horário a partir do eixo $x$.
\end{itemize}
Conforme observação anterior.

\begin{figure}
  \centering
  \includegraphics[width=0.6\textwidth,trim={2cm 11cm 3cm 160mm},clip]{figcirctrig2}
  \caption{?}
\end{figure}

Podemos variar os valores dos ângulos que para dois valores $\theta_1$ e
$\theta_2$, teremos (???????????)

\begin{figure}
  \centering
  \includegraphics[width=0.6 \textwidth,trim={3cm 165mm 2cm 6cm} ,clip]{figcirctrig3}
  \caption{?}
  \label{trigon2}
\end{figure}

Podemos agora definir o círculo trigonométrico unitário (conforme
Figura~\ref{trigon3}), que é o círculo do plano cartesiano de centro na origem
$(0, 0)$ e raio 1. O eixo $x$ é também chamado de \emph{eixo dos cossenos}, já o eixo
$y$ é também chamado de \emph{eixo dos senos}.

Considere um ponto $A$ sobre o círculo trigonométrico no primeiro quadrante e
seja $B$ a projeção desse ponto no eixo $x$.  Seja $C$ a interseção do
segmento de reta que passa por $\overline{OA}$ com a reta $x=1$ e $D=(1,0)$, ou
seja, o segmento $\overline{CD}$ encontra-se sobre a reta $x=1$. Seja $\theta$
o ângulo $A\hat{O}B$. Observe que as coordenadas de $A$ no plano cartesiano são
$(\cos\theta, \sin\theta)$. Por semelhança de triângulos (triângulos $AOB$ e
$COD$) vemos que
\[
    \frac{\,\abs{\overline{CD}}\,}{1}
  = \frac{\,\abs{\overline{CD}}\,}{\abs{\overline{OD}}}
  = \frac{\,\abs{\overline{AB}}\,}{\abs{\overline{OB}}}
  = \frac{\,\sin\theta\,}{\cos\theta},
\]
ou seja,
\[
  \abs{\overline{CD}} = \tan\theta.
\]

\begin{figure}
  \centering
  \includegraphics[scale=3]{circtrigonom}
  \caption{Círculo Trigonométrico Unitário.}
  \label{trigon3}
\end{figure}

Vemos assim que para $\theta\in(0, \pi/2)$ os valores $\sin\theta$, $\cos\theta$
e $\tan\theta$ podem ser obtidos a partir das coordenadas dos pontos $A$ e $C$:
$A=(\cos\theta, \sin\theta)$ e $C=(1, \tan\theta)$. Mas essa construção pode ser
feita para qualquer $\theta\in\R$ e dessa forma podemos definir as funções
seno, cosseno e tangente para quaisquer ângulos reais.  Por exemplo, essa
construção para um ângulo $\theta$ no segundo quadrante pode ser vista na
Figura~\ref{trigon4}.

\begin{figure}
  \centering
  \includegraphics[width=0.4\textwidth,trim={0 1cm 9cm 3cm},clip]{circ5}
  \caption{Interpretação Geométrica da Tangente de um Ângulo no Segundo Quadrante}
  \label{trigon4}
\end{figure}

Continuando a construção, podemos dividir o círculo trigonométrico unitário em
quatro partes iguais, chamadas de quadrantes. No primeiro quadrante estão os
ângulos entre 0 e \ang{90}, ou equivalentemente, entre 0 e
$\frac{\pi}{2}$. No segundo quadrante estão os ângulos entre \ang{90} e
\ang{180} (ou entre $\frac{\pi}{2}$ e $\pi$). No terceiro quadrante estão
os ângulos entre \ang{180} e \ang{270} (ou entre $\pi$ e
$\frac{3\pi}{2}$). Por fim, no quarto quadrante estão os ângulos entre
\ang{270} e $3\ang{60}$ (ou entre $\frac{3\pi}{2}$ e $2\pi$). Os ângulos
$0,\frac{\pi}{2}, \pi, \frac{3\pi}{2}$ e $2\pi$ são denominados ângulos do
eixo. Esses ângulos são notáveis e observando a Figura~\ref{trigon3}, podemos
descobrir o valor do seno, cosseno e tangente desses arcos.
A Tabela~\ref{tabelaeixos} apresenta esses valores.

\begin{table}
  \centering
  \begin{tabular}{l<{\quad}>{\quad}c<{\quad}>{\quad}c<{\quad}>{\quad}c<{\quad}>{\quad}c<{\quad}>{\quad}r}
    \hline
              & 0 & $\sfrac{\pi}{2}$ & $\pi$ & $\sfrac{3\pi}{2}$ & $2\pi$ \\ \hline
    $\sin(x)$ & 0 & 1                & 0     & -1                & 0      \\
    $\cos(x)$ & 1 & 0                & -1    & 0                 & 1      \\
    $\tan(x)$ & 0 & $\nexists$       & 0     & $\nexists$        & 0      \\ \hline
  \end{tabular}
  \caption{Seno, cosseno e tangente dos ângulos do eixo.}
  \label{tabelaeixos}
\end{table}

\begin{observation}
  \emph{A relação fundamental trigonométrica}
  A partir da Figura~\ref{trigon3}, por exemplo, podemos extrair uma relação
  trigonométrica, que é muito útil. Essa relação é conhecida como
  \emph{relação fundamental trigonométrica} e pode ser demonstrada, aplicando
  o Teorema de Pitágoras ao triângulo de hipotenusa 1 e catetos $\sin(\theta)$
  e $\cos(\theta)$.
  \[
    \sin^2(x)+ \cos^2(x)=1
  \]
  A partir dessa relação podemos obter duas outras conforme serão mostradas nos
  exercícios que seguem.
\end{observation}

\begin{example}
  Usando a relação fundamental trigonométrica mostre que:
  $\tan^2(x)+1 = \sec^2(x)$,
  quando $\cos(x) \neq 0$ e $1+ \cot^2(x) = \csc^2(x)$ se
  $\sin(x) \neq 0$.

  \solution
  Da relação fundamental trigonométrica temos:
  \begin{equation} \label{relafun}
    \sin^2(x)+\cos^2(x)=1
  \end{equation}
  Sabendo que $\cos(x) \neq 0$, podemos dividir a equação~\eqref{relafun}
  por $\cos^2(x)$. Assim,
  \[
    \frac{\sin^2(x)}{\cos^2(x)}+ 1= \frac{1}{\cos^2(x)}
  \]
  Logo,
  \[
    \tan^2(x)+1=\sec^2(x).
  \]
  Para chegarmos na outra expressão basta dividirmos a relação fundamental por
  $\sin^2(x)$.
\end{example}

%------------------------------------------------------------------------------%
\section{Fórmulas de redução ao primeiro quadrante}

Ao trabalharmos com Trigonometria e deparamo-nos com um ângulo que não se
encontra no primeiro quadrante, sempre podemos reduzi-lo de forma a encontrar o
ângulo correspondente a esse que esteja justamente no 1\textsuperscript{o} quadrante.
Usamos esse procedimento em virtude ds simetria encontrada no ciclo
trigonométrico. Mas precisamos nos atentar para o que ocorre com os sinais das
funções trigonométricas em cada quadrante. A Tabela~\ref{sinal1} e a
Figura~\ref{Sinai1} mostram os sinais em cada quadrante.

\begin{table}
  \centering
  \begin{tabular}{l<{\quad}>{\quad}c<{\quad}>{\quad}c<{\quad}>{\quad}c<{\quad}>{\quad}r}
    \hline
    &
    1\textsuperscript{o} Q &
    2\textsuperscript{o} Q &
    3\textsuperscript{o} Q &
    4\textsuperscript{o} Q    \\ \hline
    $\sin(x)$ & $+$ & $+$ & $-$ & $-$ \\
    $\cos(x)$ & $+$ & $-$ & $-$ & $+$ \\
    $\tan(x)$ & $+$ & $-$ & $+$ & $-$ \\ \hline
  \end{tabular}
  \caption{Sinais em cada quadrante.}
  \label{sinal1}
\end{table}

\begin{figure}
    \centering
    \includegraphics{sinais}
    \caption{Sinais em cada quadrante.}
    \label{Sinai1}
\end{figure}

\begin{figure}
  \centering
  \includegraphics[scale=2]{ciclo2p1}
  \caption{Redução do 2\textsuperscript{o} quadrante para o
    1\textsuperscript{o} quadrante.}
  \label{ciclo2p1}
\end{figure}

\begin{figure}
  \centering
  \includegraphics[scale=2]{terp1}
  \caption{Redução do 3\textsuperscript{o} quadrante para o
    1\textsuperscript{o} quadrante.}
  \label{ciclo2p2}
\end{figure}

\begin{figure}
  \centering
  \includegraphics[scale=2]{qp1}
  \caption{Redução do 4\textsuperscript{o} quadrante para o
    1\textsuperscript{o} quadrante.}
  \label{ciclo2p3}
\end{figure}

Se o ângulo com o qual estamos trabalhando for $y$ e ele estiver no
segundo quadrante, seu correspondente no 1\textsuperscript{o} quadrante será o
ângulo $x$ tal que $\pi - x = y$ ou $\ang{180} - x = y$. Você não deve
se ater as fórmulas, a Figura~\ref{ciclo2p1} mostra que você pode fazer
essa redução usando o círculo trigonométrico.

Se o ângulo $y$ pertencer ao terceiro quadrante, seu correspondente
$x$ no primeiro quadrante será dado por $x + \pi = y$ ou
$\ang{180} + x = y$.
A Figura~\ref{ciclo2p2} mostra que você pode fazer essa redução
usando o círculo trigonométrico.

Se o ângulo com o qual estamos trabalhando for $y$ e ele estiver no
quarto quadrante, seu correspondente no 1\textsuperscript{o} quadrante será o
ângulo $x$ tal que $2\pi - x = y$ ou $3\ang{60} - x = y$.
A Figura~\ref{ciclo2p3} mostra que você pode fazer essa redução usando o
círculo trigonométrico.

\begin{observation}
    Tente $n$
\end{observation}

\begin{figure}
  \centering
  \includegraphics[scale=2]{135}
  \caption{Redução do \ang{135} ao 1\textsuperscript{o} quadrante.}
  \label{ciclo2p1}
\end{figure}

\begin{example}
  Reduza os ângulos
  \ang{135},
  $\frac{11 \pi}{6}$ rad e
  $\frac{4\pi}{3}$ rad e calcule os valores de
  $\sin(x)$, $\cos(x)$ e $\tan(x)$ para esses ângulos.

  \solution
  \emph{\(x=\ang{135}\)}

  O arco $x=\ang{135}$ está no $2^{\circ}$ quadrante, então para
  reduzirmos ao primeiro quadrante basta usarmos a fórmula
  $\ang{180}-x=y$. Logo, $y=\ang{180}- \ang{135}=\ang{45}$.

  Para encontrarmos as relações trigonométricas para esse ângulo, devemos
  nos atentar para o sinal das funções trigonométricas no quadrante a qual
  ele pertence. Nesse caso, temos que $x=\ang{135}$ está no segundo
  quadrante. Assim,
  \begin{align*}
    \sin(\ang{135}) & = \sin(\ang{45})= \frac{\sqrt{2}}{2}   \\
    \cos(\ang{135}) & = -\cos(\ang{45})=- \frac{\sqrt{2}}{2} \\
    \tan(\ang{135}) & =- \tan(\ang{45})= -1
  \end{align*}

  \emph{\(x=\frac{11\pi}{6}\) rad}

  Sabendo que $\pi rad= \ang{180}$, então $\frac{11\pi}{6}=
  \frac{11.180}{6}=3\ang{30}$. Assim, $x=\frac{11\pi}{6} rad$ está no
  4\textsuperscript{o} quadrante. Para reduzirmos ao primeiro quadrante, basta
  usarmos a fórmula $y=3\ang{60}-x$. Logo,
  $y=3\ang{60}-3\ang{30}=\ang{30}$. Observando o sinal de cada função
  trigonométrica no $4^{\circ}$ temos:
  \begin{align*}
    \sin(3\ang{30}) & = -\sin(\ang{30}) = -\frac{1}{2}        \\
    \cos(3\ang{30}) &  = \cos(\ang{30}) =  \frac{\sqrt{3}}{2}   \\
    \tan(3\ang{30}) & = -\tan(\ang{30}) = -\frac{\sqrt{3}}{3}
  \end{align*}

  \emph{\(x=\frac{4\pi}{3}\) rad}

  Sabendo que $x=\frac{4.180}{3}=\ang{240}$, então $x$ está no
  3\textsuperscript{o}
  quadrante e para reduzirmos ao terceiro quadrante usamos a relação
  $y=x-\ang{180}$. Logo, $y=\ang{240}-\ang{180}=\ang{60}$. Assim,
  observando o sinal de cada função trigonométrica no $3^{\circ}$ temos:
  \begin{align*}
    \sin(\ang{240}) & = -\sin(\ang{60}) = -\frac{\sqrt{3}}{2} \\
    \cos(\ang{240}) & =  \cos(\ang{60}) =  \frac{1}{2}        \\
    \tan(\ang{240}) & = -\tan(\ang{60}) =  \sqrt{3}
  \end{align*}
\end{example}

%------------------------------------------------------------------------------%
\section{Gráficos de Funções Trigonométricas}

Um fenômeno é chamado periódico se o mesmo se repete sempre após o mesmo
intervalo de tempo. Um exemplo simples de fenômeno periódico é o dia,
determinado pelo movimento do Sol. A observação do movimento periódico dos
corpos celestes nos possibilitou a criação do nosso calendário. Para uma melhor
compreensão dos fenômenos periódicos, faz-se necessário o estudo das funções
periódicas.

A repetição do valor numérico de uma função em um período determinado constitui
uma função periódica. Mais precisamente, uma função $f$ é periódica de período
$T$ se para todo $x$ real
\[
  f(x+T)=f(x).
\]

Observe que conhecido o gráfico de uma função periódica em um determinado
intervalo de seu domínio, que tenha o tamanho de seu período, automaticamente
conhecemos  o gráfico em todo o domínio, uma vez que podemos repetir o gráfico
em outros intervalos de mesmo tamanho. As funções trigonométricas são
periódicas e por isso apresentaremos os seus gráficos em intervalos do tamanho
de seus períodos. O período das funções seno e cosseno é $2\pi$. O período
da função tangente é $\pi$.

As seguintes observações merecem destaque.

\begin{observation}
  As funções seno e cosseno são definidas em todo o conjunto dos
  números reais e seus valores variam entre $-1$ e 1, ou seja, a imagem
  dessas funções é o intervalo fechado de $[-1,1]$. Isso significa que o
  valor máximo que a função pode atingir é 1, enquanto o valor mínimo é
  $-1$.
\end{observation}

\begin{observation}
  É importante notar que, além de possuir valor máximo de 1 e valor
  mínimo de $-1$, as funções seno e cosseno atingem esses valores
  infinitas vezes ao longo de seus domínios.
\end{observation}

\begin{observation}
  A função $y = \cos(x)$ é uma função par, o que significa que
  $\cos(-x) = \cos(x)$. Isso pode ser facilmente observado no círculo
  trigonométrico, já que a função cosseno representa a coordenada x de um
  ponto na circunferência unitária, que é simétrica em relação ao eixo y.
  Portanto, quando o ângulo é negativo, o cosseno mantém o mesmo valor
  que quando o ângulo é positivo.
\end{observation}

\begin{observation}
  Para fazer o esboço do gráfico das funções $y=\sin(x)$ e
  $y=\cos(x)$, utilizamos os arcos do eixo no círculo trigonométrico.
\end{observation}

\begin{observation}
  A função $y = \sin(x)$ é uma função ímpar, o que significa que
  $\sin(-x) = -\sin(x)$. Isso pode ser facilmente observado no círculo
  trigonométrico.
\end{observation}

\begin{observation}
  Detalhes sobre função $y=\tan(x)$, será apresentado posteriormente,
  no entanto, vale adiantar que essa função é ímpar, pois
  \[
    \tan(-x)
    = \frac{\sin(-x)}{\cos(-x)}
    = \frac{-\sin(x)}{\cos(x)}
    = -\tan(x).
  \]
\end{observation}

As Figuras~\ref{senono} e \ref{cossenono} mostram o gráfico das funções
$y=\sin(x)$ e $y=\cos(x)$ em um período, ou seja, no intervalo $[0, 2\pi]$

\begin{figure}
  \centering
  \includegraphics[scale=1.2]{seno-1}
  \caption{O gráfico da função seno em $[0,2\pi]$.}
  \label{senono}
\end{figure}

\begin{figure}
  \centering
  \includegraphics[scale=1.2]{cos}
  \caption{O gráfico da função cosseno em $[0,2\pi]$.}
  \label{cossenono}
\end{figure}

Os próximos exemplos apresentam como construir gráficos de funções que são
transformações nas funções $y=\sin(x)$ e $y=\cos(x)$.

\begin{figure}
  \centering
  \includegraphics[scale=0.9]{exemplo1seno}
  \caption{Em azul, o gráfico da função $y=2\sin(x)+2$ e em vermelho,
    gráfico da função $y=\sin(x)$.}
  \label{exercseno12}
\end{figure}

\begin{figure}
  \centering
  \includegraphics[scale=0.9]{exerciciocosseno}
  \caption{Em azul, o gráfico da função
    $y=\frac{1}{2}\cos(x+\frac{\pi}{2})-1$ e em vermelho, gráfico da função
    $y=\cos(x)$.}
  \label{exercseno1}
\end{figure}

\begin{example}
  Esboce o gráfico da função
  \(f(x)= 2 \sin(x) +2\),
  explicitando o domínio e a imagem.

  \solution
  Note que $D(f)=\R$, uma vez que não temos nenhuma restrição para $x$.

  Para encontrarmos a imagem de $f$ devemos lembrar que
  $-1 \leq \sin(x) \leq 1$ então se multiplicarmos por 2
  e depois somarmos 2 em todos os
  termos da desigualdade chegamos que:
  \begin{align*}
    -2+2 & \leq  2 \sin(x) + 2 \leq 2+2 \\
    0    & \leq 2 \sin(x) + 2 \leq 4
  \end{align*}
  Assim, $\Im(f) = [0, 4]$.

  Uma vez que conhecemos o gráfico da função $y=\sin(x)$ podemos usar
  transformações para obtermos o gráfico de $f$. Uma vez que para obtermos
  a função $f$ multiplicamos a função $y=\sin(x)$ por 2 e depois somamos
  2 unidades, então para fazermos o esboço do gráfico de $f$ precisamos
  fazer uma expansão vertical de comprimento 2 e depois fazermos uma
  translação vertical (subimos 2 unidades no eixo $y$).
\end{example}

\begin{example}
  Esboce o gráfico da função
  \(g(x)= \frac{1}{2} \cos(x+ \frac{\pi}{2})-1\),
  explicitando o domínio e a imagem.

  \solution
  Note que $D(f)= \R$, uma vez que não temos nenhuma restrição para
  $x$.

  Para encontrarmos a imagem de $f$ devemos lembrar que $-1 \leq
  \cos\left(x+ \frac{\pi}{2} \right) \leq 1$ então se multiplicarmos por
  $\frac{1}{2}$ e depois subtraímos 1 em todos os termos dessa
  desigualdade chegamos que
  \begin{align*}
    -1-\frac{1}{2} & \leq \frac{1}{2} \cos(x+ \frac{\pi}{2}) -1 \leq \frac{1}{2} -1 \\
    - \frac{3}{2}  & \leq \frac{1}{2} \cos(x+ \frac{\pi}{2}) - \leq -\frac{1}{2}
  \end{align*}
  Assim, \(\Im(f)=\left[-\frac{3}{2},-\frac{1}{2}\right]\).

  Conhecemos o gráfico da função $y=\cos(x)$ então para obtermos o gráfico
  da função $f$ precisamos fazer três transformações na função $y=\cos(x)$:

  Contração vertical, por conta do fator $\frac{1}{2}$.

  Deslocamento horizontal, ou seja, ``andar para trás''
  \(\frac{\pi}{2}\) unidades.

  Fazer uma translação vertical, ou seja, ``descer'' uma unidade
  no eixo $y$.
\end{example}

\begin{observation}
   De modo geral, se temos uma função trigonométrica da forma $y=a+b
   \sin(kx+r)$ ou $y=a+b \cos(kx+r)$ a imagem de qualquer uma dessas funções
   é o intervalo $[a-b,a+b]$ e o período é $\frac{2\pi}{\vert k\vert}$. No
   entanto, ao invés de olharmos só para essas fórmulas é interessante
   entender o porquê disso.
\end{observation}

\begin{observation}
  A função $f(x)=\tan x$ não está definida em $-\pi/2$ e $\pi/2$, e
  consequentemente (porque $f$ tem período $\pi$), em qualquer $x$ da forma
  $x=\pi/2+k\pi,$ para $k\in\Z$, logo, o domínio da função tangente é
  $\{x\in \R;x\neq \pi/2+k\pi, \text{para todo } k \in \Z\}$ e a
\end{observation}

\begin{figure}
  \centering
  \includegraphics[width=0.8\textwidth,trim={1cm 2cm 1cm 1cm},clip]{tg}
  \caption{O gráfico da função tangente em $(-\pi/2, \pi/2)$.}
\end{figure}

%------------------------------------------------------------------------------%
\section{Outras Funções Trigonométricas}

Definimos as funções seno, cosseno e tangente que são as principais funções
trigonométricas, mas outras funções trigonométricas também são importantes:
\begin{description}
  \item[Cosecante de $x$]:  \(\csc x=\frac{1}{\sin x}\), se $\sin(x) \neq 0$.
  \item[Secante de $x$]:    \(\sec x=\frac{1}{\cos x}\), se $\cos(x) \neq 0$.
  \item[Cotangente de $x$]: \(\cot x=\frac{1}{\tan x}\), se $\tan(x) \neq 0$.
\end{description}

\begin{example}
  Pesquise sobre o domínio, a imagem, o gráfico e a inversa de cada uma das
  funções definidas nessa seção.
\end{example}

\begin{example}
  Se \(\cos(x)= -\frac{2}{5}\) e $x$ no 3\textsuperscript{o} quadrante,
  calcule o valor de $\sec(x)+\tan(x)$.

  \solution
  Sempre que tivermos o valor de uma das funções trigonométricas $\sin(x)$ ou
  $\cos(x)$, a \emph{Relação Fundamental Trigonométrica} será útil para
  encontrar a outra função. Por exemplo, nesse exercício temos o valor de
  $\cos(x)$, então podemos encontrar o valor de $\sin(x)$ usando a
  \emph{Relação Fundamental Trigonométrica}. Assim,
  \begin{align*}
    \sin^2(x) + \cos^2(x)                     & = 1                       \\
    \sin^2(x) + \left( -\frac{2}{5} \right)^2 & = 1                       \\
    \sin^2(x) + \frac{4}{25}                  & = 1                       \\
    \sin^2(x)                                 & = 1- \frac{4}{25}         \\
    \sin^2(x)                                 & = \frac{21}{25}           \\
    \sin(x)                                   & = \pm \frac{\sqrt{21}}{5}
  \end{align*}
  Uma vez que $x$ no 3\textsuperscript{o} quadrante, então
  $\sin(x) = -\frac{\sqrt{21}}{5}$, assim:
  \[
    \sec(x)
    = \frac{1}{\cos(x)}
    =-\frac{5}{2}
  \]
  e
  \[
    \tan(x)
    = \frac{\sin(x)}{\cos(x)}
    =\frac{-\frac{\sqrt{21}}{5}}{-\frac{2}{5}}=\frac{\sqrt{21}}{2}
  \]
  Consequentemente
  \[
    \sec(x)+\tan(x)
    = -\frac{5}{2}+\frac{\sqrt{21}}{2}
    = \frac{-5+\sqrt{21}}{2}.
  \]
\end{example}

\begin{example}
  Se \(\sin(x) = \frac{3}{5}\) e $x$ no 1\textsuperscript{o} quadrante,
  encontre o valor da expressão
  \[
    z = \frac{\sec(x)- cossec(x)}{\tan(x)+ \cot (x)}
  \]
  Usando a \emph{Relação Fundamental Trigonométrica} temos:
  \begin{align*}
    \sin^2(x)+ \cos^2(x)                    & = 1               \\
    \left( \frac{3}{5}\right)^2 + \cos^2(x) & = 1               \\
    \cos^2(x)                               & = 1-\frac{9}{25}  \\
    \cos^2(x)                               & = \frac{16}{25}   \\
    \cos(x)                                 & = \pm \frac{4}{5}
  \end{align*}
  Sendo $x$ no 1\textsuperscript{o} quadrante, então
  \(\cos(x)= \frac{4}{5}\), assim:
  \begin{align*}
    \sec(x) & = \frac{1}{\cos(x)}       = \frac{5}{4};                                     \\[2mm]
    \tan(x) & = \frac{\sin(x)}{\cos(x)} = \frac{\sfrac{3}{5}}{\sfrac{4}{5}} = \frac{3}{4}; \\[2mm]
    \csc(x) & = \frac{1}{\sin(x)}       = \frac{5}{3};                                     \\[2mm]
    \cot(x) & = \frac{1}{\tan(x)}       = \frac{4}{3}.
  \end{align*}
  Portanto,
  \[
    z
    = \frac{\sec(x) - \csc(x)}{\tan(x) + \cot(x)}
    = \frac{\sfrac{5}{4} - \sfrac{5}{3}}{\sfrac{3}{4} + \sfrac{4}{3}}
    = -\frac{1}{5}.
  \]
\end{example}

%------------------------------------------------------------------------------%
\section{Identidades Trigonométricas}

Algumas relações conhecidas como \emph{Identidades Trigonométricas} serão
fundamentais no curso:

\emph{1) Seno da soma}
\[
  \sin(a+b)=\sin a\cos b+\sin b\cos a.
\]

\emph{2) Cosseno da soma}
\[
  \cos(a+b)=\cos a\cos b-\sin b\sin a
\]

Para visualizar as demonstrações desses itens, assista ao
vídeo disponível no seguinte link:
\url{https://www.youtube.com/watch?v=MlCZU4zNymo}

\emph{3) Tangente da soma}
\[
  \tan(a+b)=\frac{\tan a+\tan b}{1-\tan a\tan b}.
\]

\begin{proof}
  Para demonstrar esse item, usamos a definição de função tangente e
  as expressões dos itens 1 e 2. Assim,
  \[
    \tan(a+b)
    = \frac{\sin(a+b)}{\cos(a+b)}
    = \frac{\sin a\cos b+\sin b\cos a}{\cos a\cos b-\sin b\sin a}
  \]
  Dividindo o numerador e o denominador por $\cos a\cos b$, desde que
  seja não nulo, temos
  \[
    \tan(a+b)
    = \frac{\;\dfrac{\sin a\cos b+\sin b\cos a}{\cos a\cos b}\;}
           {\;\dfrac{\cos a\cos b-\sin b\sin a}{\cos a\cos b}\;}
    = \frac{\tan a+\tan b}{1-\tan a\tan b}
  \]
  Portanto, a fórmula para a tangente da soma é:
  \[
    \tan(a+b)=\frac{\tan a+\tan b}{1-\tan a\tan b}
  \]
\end{proof}

\emph{4) Seno da diferença}
\[
  \sin(a-b)=\sin a\cos b-\sin b\cos a.
\]

\emph{5) Cosseno da diferença}
\[
  \cos(a-b)=\cos a\cos b+\sin b\sin a.
\]
Os itens 4 e 5 podem ser demonstrados trocando $b$ por $-b$ nas
expressões 1 e 2, respectivamente e usando que a função seno
é ímpar e a função cosseno é par.

\emph{6) Arco duplo: Seno}
\[
  \sin(2a) = 2\sin(a)\cos(a).
\]

\begin{proof}
  Essa fórmula é muito útil na trigonometria, pois permite calcular o
  valor do seno de um ângulo duplo em termos dos valores de seno e
  cosseno do ângulo original. E para demonstrá-la, vamos partir da
  fórmula da adição para o seno, apresentada no item 1, trocando $b$
  por $a$. Logo,
  \[
    \sin(2a)
    = \sin(a + a)
    = \sin(a)\cos(a) + \cos(a)\sin(a)
    = 2\sin(a)\cos(a)
  \]
  Portanto, a fórmula para o seno do arco duplo é
  \[
    \sin(2a) = 2\sin(a)\cos(a)
  \]
\end{proof}

\emph{7) Arco duplo: Cosseno}
\[
  \cos(2a)=\cos^2(a)-\sin^2(a)
\]

\begin{proof}
    Vamos partir da fórmula da adição para o cosseno, apresentada no item 2:
    \[
      \cos(\alpha + \beta)
      = \cos(\alpha)\cos(\beta) - \sin(\alpha)\sin(\beta)
    \]
    Substituindo $\alpha = a$ e $\beta = a$, temos
    \[
      \cos(2a)
      = \cos(a + a)
      = \cos(a)\cos(a) - \sin(a)\sin(a)= \cos^2(a)-\sin^2(a).
    \]
    Portanto, a fórmula para o seno do arco duplo é
    \begin{equation}
      \cos(2a) = cos^2(a)-\sin^2(a)
      \label{duplo}
    \end{equation}
\end{proof}

Note que isolando $\sin^2(x)$ na identidade trigonométrica
$\sin^2(a) + \cos^2(a) = 1$,
podemos reescrever a expressão \eqref{duplo} como
\[
  \cos(2a) = \cos^2(a) - \left(1 - \cos^2(a)\right)
\]
Simplificando, obtemos outra expressão para $\cos(2a)$, que depende
apenas de cosseno
\begin{equation}
  \cos(2a) = 2\cos^2(a) - 1
  \label{duplo1}
\end{equation}
Se isolarmos $\cos^2(x)$ na identidade trigonométrica
$\sin^2(a) + \cos^2(a) = 1$,
podemos reescrever a expressão \eqref{duplo} como
\begin{equation}
  \cos(2a) = 1-2\sin^2(a)
  \label{duplo2}
\end{equation}
Essas expressões serão essenciais para demonstrar as fórmulas de arco
metade que seguem.

\emph{8) Arco metade: Cosseno})
\[
  \cos^2(x) = \frac{1}{2}(1+\cos(2x)).
\]
\begin{proof}
  Começamos com a fórmula \eqref{duplo1}, $\cos(2a) = 2\cos^2(a) - 1$
  e substituímos $a$ por $\sfrac{x}{2}$ para obter
  \[
    \cos(x) = 2\cos^2\left(\frac{x}{2}\right) - 1
  \]
  Isolando \(\cos^2\left(\frac{x}{2}\right)\), temos
  \[
    \cos^2\left(\frac{x}{2}\right) = \frac{1}{2}(1 + \cos(x))
  \]
  Substituindo $\sfrac{x}{2}$ por $x$ e reescrevendo a equação em
  termos de $\cos(2x)$, temos
  \[
    \cos^2(x) = \frac{1}{2}(1 + \cos(2x))
  \]
  Portanto,
  \[
    \cos^2(x)= \frac{1}{2}(1+\cos(2x))
  \]
\end{proof}

\emph{9) Arco metade: Seno}
\[
  \sin^2(x)= \frac{1}{2}\left(1-\cos(2x)\right).
\]

\begin{proof}
    Para comprovar esse item, é possível utilizar a mesma abordagem
    utilizada anteriormente, porém aplicando agora a fórmula
    \eqref{duplo2}. Dessa forma, será possível demonstrar de maneira
    equivalente a veracidade desse item.
\end{proof}

%------------------------------------------------------------------------------%
\section{Equações Trigonométricas}

Uma equação trigonométrica é uma equação que envolve funções trigonométricas
como seno, cosseno, tangente, cotangente, secante ou cossecante de uma variável
trigonométrica. O objetivo da resolução de equações trigonométricas é encontrar
todas as soluções possíveis para a equação.

As equações abaixo são exemplos de equações trigonométricas:
\begin{gather*}
  \sin(x) = \frac{1}{2} \\[2mm]
  \cos^2(x)-\cos(x) = 0 \\[2mm]
  \cos(x)\left(\tan(x)-1\right) = 0
\end{gather*}

Para resolvermos uma equação trigonométrica, precisamos nos atentar em qual
intervalo é pedida a solução.

A resolução de equações trigonométricas pode ser feita por meio de diversas
técnicas, dependendo da complexidade da equação. Alguns dos métodos mais comuns
incluem:
\begin{enumerate}
  \item Equações simples: são aquelas que envolvem apenas uma função
    trigonométrica. Nesse caso, é possível usar propriedades básicas das
    funções trigonométricas para encontrar as soluções.

  \item Equações quadráticas: são aquelas que envolvem o quadrado de uma função
    trigonométrica. Nesse caso, é possível fazer uma substituição adequada para
    transformar a equação em uma equação quadrática comum, que pode ser
    resolvida usando as técnicas usuais.

  \item Equações com produto nulo: são aquelas em que uma ou mais funções
    trigonométricas multiplicadas entre si resultam em zero. Nesse caso, é
    possível usar a propriedade do produto nulo para encontrar as soluções.

  \item Uso de identidades: muitas vezes, as equações trigonométricas podem ser
    resolvidas por meio da aplicação de identidades trigonométricas, como as
    identidades de soma e diferença, identidades de produto e identidades de
    duplo ângulo.
\end{enumerate}

Em geral, a resolução de equações trigonométricas requer um bom conhecimento
das propriedades e identidades das funções trigonométricas, bem como das
técnicas algébricas usadas para resolver equações. Vamos resolver alguns
exemplos.

\begin{figure}
  \centering
  \includegraphics[scale=2]{equa1png}
  \caption{Ilustração da solução da equação $\sin(\theta) =\frac{\sqrt{2}}{2}$.}
  \label{equa1}
\end{figure}

\begin{example}
  Resolva a seguinte equação trigonométrica \(\sin(\theta) = \frac{\sqrt{2}}{2}\).

  \solution
  \emph{No intervalo $[0, 2\pi]$}

  Note que, nesse caso, queremos encontrar os arcos no intervalo
  $[0, 2\pi]$, cujo seno vale $\sfrac{\sqrt{2}}{2}$. Sabemos que
  $\sin(\theta)$ é positivo no primeiro e segundo quadrante. No primeiro
  quadrante o ângulo é $\sfrac{\pi}{4}$ (usamos sempre o arco do 1\textsuperscript{o}
  como referência). Para encontrar o arco no segundo quadrante, usamos a
  expressão
  \[
    \pi-\frac{\pi}{4} = \frac{3\pi}{4},
  \]
  conforme mostra a Figura~\ref{equa1}.

  Portanto, a solução da equação pedida é
  \[
    S = \left\{ \frac{\pi}{4}, \frac{3\pi}{4} \right\}.
  \]

  \emph{Solução geral}

  A solução geral é dada por:
  \[
    S =
    \left\{
       x \in \R | x=\frac{\pi}{4}+2k\pi
      \; \text{ ou } \;
      x = \frac{3\pi}{4} +2k\pi, \quad k \in \Z
    \right\}.
  \]
\end{example}

\begin{example}
  Resolva a equação
  \(2\cos(\frac{\theta}{3}) - \sqrt{3} = 0\),
  onde $0 \leq \theta \leq 3\pi$.

  \solution
  A equação
  \(2\cos\left(\frac{\theta}{3}\right) - \sqrt{3} = 0\)
  é equivalente a
  $\cos\left(\frac{\theta}{3}\right) = \frac{\sqrt{3}}{2}$.
  Como $0 \leq \theta \leq 3\pi$, então,
  $0 \leq \frac{\theta}{3} \leq \pi$.
  Uma vez que o único arco no intervalo $[0, \pi]$,
  que possui cosseno igual a
  $\sfrac{\sqrt{3}}{2}$ é $\sfrac{\pi}{6}$,
  temos que
  \begin{align*}
    \frac{\theta}{3} & = \frac{\pi}{6}  \\
    \theta           & = \frac{\pi}{2}.
  \end{align*}
  Portanto,
  \[
    S = \left\{\frac{\pi}{2} \right\}.
  \]
\end{example}

\begin{figure}
  \centering
  \includegraphics[scale=2]{equa2}
  \caption{Ilustração da solução da equação $2\cos(x)= -1$.}
  \label{equa2}
\end{figure}

\begin{example}
  Resolva a equação $2\cos(x)= -1$ no intervalo $[0,2\pi]$.

  \solution
  Primeiro, vamos isolar o $\cos(x)$ dividindo ambos os lados por 2:
  \[
    \cos(x)= -\frac{1}{2}
  \]

  Sabemos que o cosseno é negativo no segundo e terceiro quadrante, para
  encontrarmos quais são esses arcos, pegamos como referência o arco no
  primeiro quadrante que tem cosseno igual a $\frac{1}{2}$, isto é,
  $\frac{\pi}{3}$.

  Conforme mostra a Figura~\ref{equa2}, o arco no segundo quadrante é
  \[
    \pi-\frac{\pi}{3}= \frac{2\pi}{3}
  \]
  e o arco no terceiro quadrante é
  \[
    \pi+\frac{\pi}{3}= \frac{4\pi}{3}.
  \]
  Assim,
  \[
    \cos(x) = -\frac{1}{2}
  \]
  ocorre quando $x$ é um ângulo de
  \[
    \frac{2\pi}{3}
    \quad\text{ ou }\quad
    \frac{4\pi}{3}
  \]
  (ou seus equivalentes negativos).
  Portanto, a solução da equação no intervalo $[0, 2\pi]$ é
  \[
    S= \left\{\frac{2\pi}{3}, \frac{4\pi}{3} \right\}
  \]
\end{example}

\begin{example}
  Resolva a equação
  \[
    \left(\sin(\theta) -\frac{1}{2} \right)
    \left(\tan(\theta) -1 \right) = 0,
  \]
  para $0 \leq \theta < \sfrac{\pi}{2}$.

  \solution
  Essa equação é do tipo produto nulo. Sabemos que produto $ab$ só é zero se
  $a = 0$ ou $ b = 0$.
  Portanto:
  \[
    \sin(\theta) -\frac{1}{2} = 0
    \quad\text{ ou }\quad
    \tan(\theta) -1 = 0
  \]
  Analisando cada caso, separadamente
  \begin{align*}
    \sin(\theta) -\frac{1}{2} & =0              \\
    \sin(\theta)              & =\frac{1}{2}    \\
    \theta                    & = \frac{\pi}{6}
  \end{align*}
  ou
  \begin{align*}
    \tan(\theta) -1 & = 0            \\
    \tan(\theta)    & = 1            \\
    \theta          & =\frac{\pi}{4}
  \end{align*}
  Assim, a solução da equação pedida é:
  \[
    S = \left\{ \frac{\pi}{6}, \frac{\pi}{4} \right\}.
  \]
\end{example}

\begin{example}
  Resolva a equação  $2\sin^2 x - \sin x - 1 = 0$ no intervalo $[0, 2\pi]$.

  \solution
  Neste caso, temos uma equação quadrática trigonométrica que pode ser
  resolvida através da mudança de variável $y = \sin(x)$. Dessa forma, obtemos
  a equação do segundo grau $2y^2  - y - 1 = 0$, cujas raízes são $y = 1$ e
  $y = -\frac{1}{2}$. Portanto, podemos encontrar os valores de $x$
  correspondentes da seguinte maneira:

  Para $y = 1$, temos $\sin(x) = 1$, o que implica em $x = \sfrac{\pi}{2}$.

  Para $y = -\sfrac{1}{2}$, temos $\sin(x) = -\sfrac{1}{2}$.
  Isso ocorre em dois
  quadrantes, 3\textsuperscript{o} e 4\textsuperscript{o} quadrantes.
  Portanto, os valores
  possíveis de $x$ são
  \[
    \pi + \frac{\pi}{6} = \frac{7\pi}{6}
    \qquad \text{(3\textsuperscript{o} Q)}
  \]
  ou
  \[
    2\pi - \frac{\pi}{6} = \frac{11\pi}{6}
    \qquad \text{(4\textsuperscript{o} Q)}.
  \]
  Assim o conjunto solução da equação original é
  \[
    S = \left\{ \frac{\pi}{2}, \frac{7\pi}{6}, \frac{11\pi}{6} \right\}
  \]
\end{example}

%------------------------------------------------------------------------------%
\section{Funções Trigonométricas Inversas}

As funções seno, cosseno e tangente não são bijetoras, mas uma restrição no
domínio das mesmas nos possibilita definir suas inversas. Essa escolha de
domínio não é única, mas adotaremos as escolhas padrões, que são os intervalos
que contém os ângulos notáveis e cujas imagens das funções restritas são iguais
às imagens das funções sem restrição. Os gráficos das funções inversas são
obtidos a partir dos gráficos das funções originais através da reflexão do
gráfico da função original em torno da reta $y=x$. Por isso apresentaremos três
imagens: o gráfico das funções trigonométricas restritas, esse gráfico
juntamente com sua reflexão e o gráfico das funções trigonométricas inversas.

%------------------------------------------------------------------------------%
\subsection{Arco Seno}

A função
\[
  \sin\colon \left[-\frac{\pi}{2}, \frac{\pi}{2}\right] \to [-1, 1]
\]
é bijetora e sua inversa é denominada arco seno
\[
  \arcsin\colon [-1, 1] \to \left[-\frac{\pi}{2}, \frac{\pi}{2}\right].
\]
Por definição,
\[
  \arcsin x=y \qquad\Leftrightarrow\qquad \sin y=x.
\]

Por serem inversas uma da outra, essas duas funções satisfazem:
\begin{gather*}
  \sin(\arcsin(x))=x, \quad\forall\quad x~\in [-1,1]. \\[2mm]
  \arcsin(\sin(x))=x, \quad\forall\quad x~\in \left[-\frac{\pi}{2}, \frac{\pi}{2}\right].
\end{gather*}

Para encontrarmos o gráfico da função arcosseno, vamos analisar o  gráfico da
função seno em \(\left[-\sfrac{\pi}{2}, \sfrac{\pi}{2}\right]\) como dado a seguir:

\begin{figure}
  \centering
  \includegraphics[width=0.6\textwidth,trim={1cm 10cm 12cm 3cm},clip]{sen1}
  \caption{???}
\end{figure}

A reflexão do gráfico de seno em torno no eixo $x$:

\begin{figure}
  \centering
  \includegraphics[width=0.6\textwidth,trim={1cm 4cm 12cm 2cm},clip]{sen2}
  \caption{???}
\end{figure}

E finalmente, o gráfico do arco seno:

\begin{figure}
  \centering
  \includegraphics[width=0.6\textwidth,trim={1cm 6cm 12cm 3cm},clip]{sen4}
  \caption{???}
\end{figure}

\begin{example}
  Calcule o valor de $\arcsin x$ para $x$ assumindo os seguintes valores:
  \[
    -\frac{\sqrt{3}}{2}, \quad
    0,                   \quad
    \frac{\sqrt{2}}{2},  \quad
    \frac{1}{2},         \quad
    1
  \]

  \solution
  Temos que:
  \begin{gather*}
    \arcsin\left(-\frac{\sqrt{3}}{2}\right) = -\frac{\pi}{3} \\
    \arcsin(0)                              = 0              \\
    \arcsin\left(\frac{\sqrt{2}}{2}\right)  = \frac{\pi}{4}  \\
    \arcsin\left(\frac{1}{2}\right)         = \frac{\pi}{6}  \\
    \arcsin(1)                              = \frac{\pi}{2}
  \end{gather*}
  Vale lembrar que o valor de $\arcsin(x)$ está restrito ao intervalo
  \[
    \left[-\frac{\pi}{2}, \frac{\pi}{2}\right].
  \]
  Por exemplo, no caso de
  \[
    \arcsin\left(-\frac{\sqrt{3}}{2}\right),
  \]
  o valor $-\frac{\pi}{3}$ está
  nesse intervalo, mas o valor $\frac{2\pi}{3}$ também é uma solução da equação
  \[
    \sin(x)=-\frac{\sqrt{3}}{2},
  \]
  porém não está no intervalo
  \[
    \left[-\frac{\pi}{2},\frac{\pi}{2}\right].
  \]
  Por isso, é importante sempre verificar se a solução encontrada está
  dentro do intervalo de definição de $\arcsin(x)$.
\end{example}

%------------------------------------------------------------------------------%
\subsection{Arco Cosseno}

A função
\[
  \cos\colon [0, \pi] \to [-1, 1]
\]
é bijetora e sua inversa é denominada arco cosseno:
\[
  \arccos\colon [-1, 1] \to [0, \pi].
\]
Por definição,
\[
  \arccos x = y \Leftrightarrow \cos y = x.
\]

Por serem inversas uma da outra, essas duas funções satisfazem
\begin{gather*}
  \cos(\arccos(x))=x, \quad\forall\quad x~\in [-1, 1]. \\
  \arccos(\cos(x))=x, \quad\forall\quad x~\in [0, \pi].
\end{gather*}

A seguir o gráfico da função cosseno em $[0, \pi]$
e de sua reflexão em torno de $y=x$.

\begin{figure}
  \centering
  \includegraphics[width=0.4\textwidth,trim={1cm 4cm 12cm 2cm},clip]{cos2}
  \qquad
  \includegraphics[width=0.4\textwidth,trim={1cm 3cm 12cm 1cm},clip]{cos3}
  \caption{???}
\end{figure}

O gráfico do arco cosseno.

\begin{figure}
  \centering
  \includegraphics[width=0.5\textwidth,trim={1cm 6cm 20cm 4cm},clip]{cos4}
  \caption{???}
\end{figure}

\begin{example}
  Calcule o valor de $\arccos x$ para $x$ assumindo os seguintes valores:
  \[
    -\frac{\sqrt{3}}{2}, \quad
    0,                   \quad
    \frac{\sqrt{2}}{2},  \quad
    \frac{1}{2},         \quad
    1
  \]

  \solution
  Temos que:
  \begin{gather*}
    \arccos\left(-\frac{\sqrt{3}}{2}\right)=\frac{5\pi}{6}. \\
    \arccos(0)=\frac{\pi}{2}.                               \\
    \arccos\left(\frac{\sqrt{2}}{2}\right)=\frac{\pi}{4}.   \\
    \arccos\left(\frac{1}{2}\right)=\frac{\pi}{3}.          \\
    \arccos(1)=0.
  \end{gather*}
  Vale lembrar que o valor de $\arccos(x)$ está restrito ao intervalo $[0, \pi]$.
  Por exemplo, no caso de
  \[
    \arccos\left(-\frac{\sqrt{3}}{2}\right),
  \]
  o valor
  $\sfrac{7\pi}{6}$ é uma solução da equação
  \[
    \cos(x)=-\frac{\sqrt{3}}{2},
  \]
  porém não está no intervalo $[0, \pi]$.
  Por isso, é importante sempre verificar se a
  solução encontrada está dentro do intervalo de definição de $\arccos(x)$.
\end{example}

%------------------------------------------------------------------------------%
\subsection{Arco Tangente}

A função
\[
  \tan\colon (-\pi/2, \pi/2) \to \R
\]
é bijetora e sua inversa é denominada arco tangente
\[
  \arctan\colon \R \to (-\pi/2, \pi/2).
\]
Por definição,
\[
  \arctan x=y \Leftrightarrow \tan y=x.
\]

Por serem inversas uma da outra, essas duas funções satisfazem:
\begin{align*}
  \tan(\arctan(x))=x, \quad\forall\quad x~\in \R. \\
  \arctan(\tan(x))=x, \quad\forall\quad x~\in \left(-\frac{\pi}{2}, \frac{\pi}{2}\right).
\end{align*}

A seguir o gráfico da função tangente em
\(\left(-\sfrac{\pi}{2}, \sfrac{\pi}{2}\right)\)
e de sua reflexão em torno de $y=x$

\begin{figure}
  \centering
  \includegraphics[width=0.4\textwidth,trim={1cm 2cm 12cm 2cm},clip]{tg1} \qquad
  \includegraphics[width=0.4\textwidth,trim={1cm 1cm 10cm 1cm},clip]{tg2}
  \caption{???}
\end{figure}

O gráfico do arco tangente.

\begin{figure}
  \centering
  \includegraphics[width=0.6\textwidth,trim={1cm 6cm 10cm 3cm},clip]{tg4}
  \caption{???}
\end{figure}

\begin{example}
  Calcule o valor de $\arctan x$ para $x$ assumindo os seguintes valores:
  \[
    -\frac{\sqrt{3}}{3}, \quad
    0,                   \quad
    \sqrt{3},            \quad
    1
  \]

  \solution
  Temos que:
  \begin{gather*}
    \arctan\left(-\frac{\sqrt{3}}{3}\right)=-\frac{\pi}{6}. \\
    \arctan\left(0\right)=0.                                \\
    \arctan\left(\sqrt{3}\right)= \frac{\pi}{3}.            \\
    \arctan\left(1\right)=\frac{\pi}{4}.
  \end{gather*}
  Vale lembrar que o valor de $\arctan(x)$ está restrito ao intervalo
  \[
    \left(-\frac{\pi}{2},\frac{\pi}{2}\right).
  \]
  Por exemplo, no caso de
  \[
    \arctan\left(-\frac{\sqrt{3}}{3}\right),
  \]
  a solução encontrada
  \[
    -\frac{\pi}{6}
  \]
  está dentro do intervalo
  \[
    \left(-\frac{\pi}{2},\frac{\pi}{2}\right),
  \]
  porém é importante sempre verificar se a solução encontrada está dentro
  do intervalo de definição de $\arctan(x)$.
\end{example}

%------------------------------------------------------------------------------%

\input{07/exercicios}

%------------------------------------------------------------------------------%
